No blockers.

Pending verification: `./gradlew assembleDebug` not yet run after qualifier removal. Expected to pass — all changes are straightforward DI simplifications with no new bindings introduced.
